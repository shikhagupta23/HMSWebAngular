import { OnlyCharacters } from './only-characters';

describe('OnlyCharacters', () => {
  it('should create an instance', () => {
    const directive = new OnlyCharacters();
    expect(directive).toBeTruthy();
  });
});
